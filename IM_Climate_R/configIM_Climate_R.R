# Install helper packages
install.packages("devtools")
install.packages("roxygen2")
library(devtools)
#install_github("devtools", "hadley")

# Setup package and configure documentation generation via ROxygen2 - use Build --> Configure Build Tools

# 

